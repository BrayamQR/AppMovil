<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/btnMenu"
        android:icon="@drawable/baseline_menu"
        android:title="Menu"
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/btnInicio"
                android:icon="@drawable/baseline_home"
                android:title="Inicio"
                />
            <item
                android:id="@+id/btnMgUser"
                android:icon="@drawable/baseline_user"
                android:title="Usuarios"
                />
            <item
                android:id="@+id/btnCerrar"
                android:icon="@drawable/baseline_exit_to_app"
                android:title="Cerrar Sesión"
                />

        </menu>
    </item>

</menu>